{
  "name": "simulacao-populacional",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --host",
    "dev:nodemon": "nodemon --watch \"index.html\" --watch \"styles.css\" --watch \"script.js\" --exec \"vite --host\"",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "vite": "^7.1.0"
  }
}
